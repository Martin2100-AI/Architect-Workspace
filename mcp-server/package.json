{
  "name": "mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "A from-scratch MCP server, built step by step.",
  "scripts": {
    "start": "ts-node --transpile-only src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
