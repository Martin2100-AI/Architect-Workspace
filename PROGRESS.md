# PROGRESS.md

Tracks completed implementation changes. Format and hard-gate rules defined in root [CLAUDE.md](CLAUDE.md) — "Logging, Reporting & Progress Tracking". Entries are appended per session, tagged with that session's ID; only the owning session may edit or audit its own entries.

## Foundation

- [x] Scaffold approved foundation folder structure
  - Date: 2026-08-03
  - Session: CC-20260803-k7q2
  - What changed: Created `backend/`, `frontend/`, `directives/`, `tests/`, `docs/` top-level trees (NOW-status subfolders only, per the approved architecture proposal), a `README.md` in each new major folder, `docs/ARCHITECTURE.md`, and this file. No product code, no dependencies, no changes to `CLAUDE.md` or `.claude/`.
  - Verification: `Get-ChildItem -Recurse -Directory` (see session transcript) confirms all 17 listed paths exist; `.claude/` and `CLAUDE.md` unchanged. `tsc --noEmit` not applicable — no TypeScript code shipped this change.
  - Notes: No project requirements doc has been located yet — this structure is derived solely from CLAUDE.md's own Folder Responsibilities section, not confirmed product requirements (see `docs/ARCHITECTURE.md` "Assumptions logged"). Deliberately skipped: `scripts/`, `nginx/`, `tmp/`, `preview-db-init/` (LATER), `execution/` and `system/` (legacy/generated, DO-NOT-TOUCH per CLAUDE.md Folder Responsibilities), root `intelligence/` and `backend/src/{services/agents,intelligence,scripts,seeds}` (CONDITIONAL, scope unconfirmed). Assumption logged: `frontend/` created alongside `backend/` despite prior NOW/LATER ambiguity — reversible, local blast radius, no governance boundary crossed, per Default Resolution Strategy. No git repository exists in this working directory yet, so no commit body carries the equivalent of an autonomy-log entry; this note is the substitute per CLAUDE.md's stated fallback ("until that writer exists, include the same information in the commit message body and PROGRESS.md note").

- [x] Add root-level README.md
  - Date: 2026-08-03
  - Session: CC-20260803-k7q2
  - What changed: Created `README.md` at repo root — project front door pointing to `CLAUDE.md` (governance), `docs/ARCHITECTURE.md` (structure), and each subfolder README. "What this is" and "Getting started" sections left as explicit placeholders since no project definition or runnable code exists yet.
  - Verification: `Test-Path README.md` returns `True`; file content reviewed inline, no other file touched.
  - Notes: Intentionally not populated with product description — would require inventing the product, which is out of scope per standing instruction. Revisit once the requirements doc/brief is located.

## Skills Lab

- [x] Scaffold `data-quality-gate` skill and sample dataset for Week 1 Agent Skills lab
  - Date: 2026-08-03
  - Session: CC-20260803-m3vq
  - What changed: Created `.claude/skills/` (did not previously exist) and `.claude/skills/data-quality-gate/SKILL.md` (procedural instruction body: requires a dataset path, uses a supplied quality contract when present, checks schema/freshness/volume/key-uniqueness/duplicates/required-fields/nulls/numeric rules, outputs a Check/Evidence/Status/Recommended-Action table, ends with PASS/WARN/FAIL and PUBLISH/BLOCK, explicitly never modifies source data; no `allowed-tools` field yet per instructions). Also created `skill-lab/orders.csv` (12 sample order rows with one intentionally duplicated `order_id`, one blank `region`, one negative `revenue`, one `load_timestamp` >48h old) and `skill-lab/quality-contract.md` (uniqueness/required/positive-revenue/24h-freshness/min-10-rows rules) for the skill to validate against in a later step. Skill was not invoked; nothing was committed.
  - Verification: `Test-Path` confirms `.claude/skills/data-quality-gate/SKILL.md`, `skill-lab/orders.csv`, and `skill-lab/quality-contract.md` all exist; contents reviewed inline. `tsc --noEmit` not applicable — no TypeScript shipped.
  - Notes: `skill-lab/` is a new top-level folder not listed in CLAUDE.md's Folder Responsibilities table; logged as a reversible, local-blast-radius assumption for this lab exercise per the Default Resolution Strategy — no governance boundary crossed. `.claude/skills/` is DRI-owned per CLAUDE.md ("Claude Code Configuration Ownership"); that rule requires DRI review before *merge*, not before creation, and no commit/merge occurred this session, so no escalation was triggered.

- [x] Harden `data-quality-gate` trigger scope, extract reference doc, add trigger-test prompts (catch-up entry)
  - Date: 2026-08-03
  - Session: CC-20260803-t5rn
  - What changed: Tightened `.claude/skills/data-quality-gate/SKILL.md`'s frontmatter `description` to explicit positive triggers (dataset/CSV/ETL-output validation, publish-readiness) and explicit negative triggers (plain SQL, dashboard design, metric calculation alone do not qualify); added a "When this applies" scope section; moved the eight per-check definitions out of `SKILL.md` into new `.claude/skills/data-quality-gate/references/quality-checks.md` and pointed to it from Step 4. Also created `skill-lab/data-quality-gate-tests.md` (3 trigger prompts, 3 non-trigger prompts, expected-output checklist) and ran three live trigger tests in-session (natural-language ask, direct `/data-quality-gate` invocation, and a plain "write SQL" negative test) — all three matched expectations. The dataset (`skill-lab/orders.csv`) and contract were read-only throughout; not modified. Nothing committed.
  - Verification: Skill re-invoked twice in-session against unchanged `skill-lab/orders.csv` + `skill-lab/quality-contract.md` and produced the same FAIL/BLOCK verdict both times (duplicate `ORD-1006` key, blank `region` on `ORD-1004`, negative `revenue` on `ORD-1005`, stale `load_timestamp` on `ORD-1008`); the SQL-only prompt did not invoke the skill. File contents reviewed inline. `tsc --noEmit` not applicable — no TypeScript shipped.
  - Notes: Filed late — this entry is a catch-up per CLAUDE.md's "Catch-up rule" (Logging section), covering work done earlier in this session before a PROGRESS.md entry was written for it. No governance boundary crossed (skill authoring is implementation-level per Autonomy Model).

- [x] Add `etl-failure-triage` skill and sample orders-pipeline failure scenario for Week 2 Agent Skills lab
  - Date: 2026-08-03
  - Session: CC-20260803-t5rn
  - What changed: Created `.claude/skills/etl-failure-triage/SKILL.md` (requires a log/run-output/failure description, reads run metadata when supplied, separates facts from hypotheses, requires an evidence citation per candidate cause, ranks causes, gives one safe diagnostic next step per cause; hard constraints: never modify pipeline code, never rerun jobs, never claim a root cause without evidence; output sections: Incident Summary, Evidence, Ranked Causes, Next Tests, Escalation Recommendation) and `.claude/skills/etl-failure-triage/references/common-failures.md` (catalog of 7 common ETL/ELT failure signatures — schema mismatch, mapping/conversion failure, retry exhaustion, timeout, auth/permission, resource exhaustion, upstream drift — with typical evidence fingerprints). Also created a believable sample incident tied to the existing orders dataset: `skill-lab/orders-pipeline-failure.log` (structured JSON log lines for job `orders_ingest_daily`, showing a schema-validation flag on `region = LATAM` for row `ORD-1012`, a `map_region_code` lookup-table miss against `region_code_map v12`, three identical failed attempts, and retry exhaustion) and `skill-lab/pipeline-run-metadata.md` (run/attempt/row-count summary, `region_code_map` reference-data state, and 5-run prior history showing the first failure after 4 consecutive successes). Skill was not invoked and nothing was committed, per instructions.
  - Verification: `Test-Path`-equivalent file reads confirm all 4 files exist at the stated paths; contents reviewed inline for internal consistency (same `correlation_id` across log and metadata, same offending value `LATAM` and row `ORD-1012` in both). `tsc --noEmit` not applicable — no TypeScript shipped.
  - Notes: `.claude/skills/` is DRI-owned per CLAUDE.md; this is skill creation, not merge, so no DRI review trigger yet. No governance boundary crossed — skill authoring and sample fixture creation are implementation-level, reversible, and local-blast-radius per the Autonomy Model.

## Colaberry Platform Integration

- [x] Connect this folder to the Colaberry platform (catch-up entry)
  - Date: 2026-08-17
  - Session: CC-20260817-q4mx
  - What changed: Created `.colaberry/connect.txt` containing a one-time pairing token (user-supplied, expires in 7 days, grants no access on its own), staged only that file (leaving `hello_claude.py`, `workflow-plan.md`, and `.claude/skills/progress-entry/` untouched), committed as "Connect this folder to Colaberry" (`e582367`), and pushed to `origin/main` (`github.com/Martin2100-AI/Architect-Workspace`, remote was already configured).
  - Verification: `git push` output confirmed `14263f8..e582367 main -> main` and `branch 'main' set up to track 'origin/main'`.
  - Notes: Filed late — this is a catch-up entry per CLAUDE.md's Catch-up rule; this session did not mint a Session ID or read `PROGRESS.md` before starting work. No governance boundary crossed (reversible, local blast radius); explicit user confirmation was obtained via `AskUserQuestion` before both the file creation and the push.

- [ ] STORY-000: Build the Keysy Command Center (Overview checkpoint — build paused for review)
  - Date: 2026-08-17
  - Session: CC-20260817-q4mx
  - What changed: Built the Command Center entry point at repo root (`index.html`, `assets/cc-theme.css`, `assets/cc.css`, `assets/cc-data.js`, `assets/cc-app.js`). Implements the header/nav shell, the hash router, the Sample/Real data-mode toggle (defaults to Real), the "Data as of" staleness stamp (reads `.colaberry/manifest.json`, honestly reports "never synced" since that file doesn't exist yet), and a fully working Overview tab sourced from `plan.project`, `plan.schedule`, and `progress.totals` read at runtime via `fetch()` — no plan/progress content is hard-coded. The other 8 tabs (Outcomes, Users & Use Case, Guardrails, Systems, Project Management, AI Agents, Knowledge Base, Data Model) are reachable from the nav and render a plain "not built yet — say build the rest" placeholder, per the brief's staged build instruction. A pause banner is shown on Overview per the same instruction. `.colaberry/plan.json`, `.colaberry/progress.json`, and `.colaberry/manifest.json` do not exist in this repo yet (no sync has happened from the portal), so Real mode currently renders honest empty states throughout Overview; this is expected, not a bug.
  - Verification: Playwright smoke test against a local static server (`npx serve` on the repo root) confirmed: page title and nav render; the "Data as of" stamp correctly reads "No data has ever been synced from the portal yet"; the pause banner is present; all 8 not-built tabs render their placeholder instead of a blank page; the Sample/Real toggle correctly shows/hides the sample banner and populates believable sample data only in Sample mode; zero `pageerror` (uncaught JS exceptions) — the only console entries were the expected 404s for the three not-yet-synced data files. Screenshots reviewed inline (`overview.png`, `overview-sample.png`).
  - Notes: This is the Overview-checkpoint state, per the brief's explicit "show me Overview first and stop" instruction — not a finished story. STORY-000's 5 acceptance criteria are not yet claimed true and `.colaberry/progress.json` has not been created; that happens at Step 3, only once genuinely true after the remaining 8 tabs are built. Not yet pushed — held locally pending user review of the Overview checkpoint.

- [x] STORY-000: Build the Keysy Command Center (remaining 8 tabs + Step 3 finish)
  - Date: 2026-08-17
  - Session: CC-20260817-q4mx
  - What changed: Built the remaining 8 tabs (Outcomes, Users & Use Case, Guardrails, Systems, Project Management, AI Agents, Knowledge Base, Data Model), each reading `.colaberry/plan.json`/`progress.json` at runtime with an honest empty state when data is missing, and each card/row drilling down one level via a two-part hash router (`#/tab` and `#/tab/id`). Guardrails cross-references `plan.requirements[].fulfilled_by` against `progress.stories[].verification.state` to show enforced/in-progress/not-yet-enforced. Systems always renders a grey "not checked from here" indicator, never fabricating a connection. Project Management renders a proportional CSS Gantt from `plan.releases[]` plus a task table with due-date slippage (`due_on` vs `due_baseline_on`). AI Agents reads `plan.agents[]`, falling back to grouping `plan.stories[].owner` (labeled "owners, not scoped agents") when no agent roster exists, and shows "no runs recorded" rather than a fabricated success rate. Knowledge Base renders a requirements traceability table (flagging `must`-priority requirements with an empty `fulfilled_by` as a gap row) plus a deterministic keyword-search chat panel over the loaded plan data that cites which tab an answer came from, or says it can't answer — there is no LLM backing it, by design (static page, no API key to hold). Data Model presents a proposed entity/field/relationship list authored by working through the 18 requirements, explicitly banered "starting point, not the answer — no tables created" per that section's own instructions; unlike the other 8 tabs this content is not read from plan.json, since it is design-review output rather than project data. Also fixed the Overview tab's schedule/count cards to drill down (they previously didn't, which would have made Done-means criterion 1 false), fixed truncated card text cutting mid-word, and split the original single `assets/cc-app.js` (which had grown to 749 lines) into `assets/cc-ui.js` (shared shell/router, 190 lines) plus one `assets/cc-tab-*.js` module per tab (32-98 lines each), each registering into a `window.CCTabs` registry — brings every file under this repo's CLAUDE.md 300-line soft target. Created `.colaberry/progress.json` with the STORY-000 entry per Step 3, all 5 Done-means criteria judged true.
  - Verification: Playwright walked all 9 tabs in both Real and Sample mode against a local static server (`npx serve`) — zero `pageerror`s in either mode; drilldown confirmed on every tab (card or row click changes the hash and renders a detail view); Sample/Real toggle confirmed correct on every tab; knowledge-base chat panel confirmed answering a real query with a tab citation and correctly declining an unanswerable one. Ran the brief's own litmus test: dropped a temporary real `.colaberry/plan.json` with 2 stories, confirmed both rendered in Project Management, removed one story from the file, reloaded, confirmed only 1 remained — proves the data-driven tabs are genuinely runtime-fetched, not hard-coded. Temporary litmus-test `plan.json` was deleted afterward (not shipped); `.colaberry/progress.json` (the STORY-000 entry only) is shipped, since Step 3 explicitly calls for it. All 8 new module files pass `node --check`.
  - Notes: Criterion 3 ("content comes from plan.json/progress.json ... rather than hard-coded") is marked true in the scoped sense of the 8 plan-driven tabs, verified by the litmus test above; the Data Model tab's entity list is deliberately authored content per that section's own brief instructions ("derive from requirements... show me the model before you create the tables"), not live plan data, and discloses this in-app via its own banner. Flagging this judgment call explicitly rather than silently treating it as a blanket pass. `.colaberry/plan.json` and `.colaberry/manifest.json` still do not exist in this repo (platform-owned, written on sync per the brief) — Real mode will show empty states on all 9 tabs until the portal syncs, which is expected. Step 4 (GitHub Pages) not yet done — separate optional step.

## Keysy Home Buying App

- [ ] STORY-001: User Registration and Login (Step 1 — backend walking skeleton)
  - Date: 2026-08-17
  - Session: CC-20260817-r8w5
  - What changed: Initialized `backend/` as a real Node.js + Express + TypeScript project (`package.json`, `tsconfig.json`, `jest.config.js`), added `src/config/env.ts` (`PORT`/`NODE_ENV` from env vars, no hardcoded values), `src/app.ts` (`createApp()` factory exporting an Express app with a single `GET /health` route returning `{status:"ok"}`), `src/server.ts` (entry point calling `createApp().listen()`), and `src/app.test.ts` (supertest happy-path test for `/health`). No auth logic, no database, no user model yet — this is purely the runnable foundation the auth endpoints get built on next, per this session's paced, one-step-at-a-time working agreement with the user.
  - Verification: `npm install` succeeded (415 packages, 0 vulnerabilities); `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed (1/1, `GET /health` returns 200 `{status:"ok"}`).
  - Notes: `node_modules` and `dist` were already gitignored at repo root, no `.gitignore` change needed. STORY-001 is in progress, not complete — remaining steps: user model, signup endpoint, login endpoint, password reset. REQ-001 mentions Google/Apple signup in addition to email; this story's acceptance criteria only test email signup/login/reset, and OAuth would need provider credentials not available here, so Google/Apple is being deferred as an explicit follow-up rather than silently built as a stub — flagged to the user, not yet objected to.

- [ ] STORY-001: User Registration and Login (Step 2 — User model / data contract)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: Added Sequelize + Postgres driver (`pg`, `pg-hstore`) as runtime deps and `sqlite3` as a test-only devDependency; `backend/src/config/database.ts` (`createSequelize()` factory pointed at Postgres via env vars — host/port/db/user/password, no hardcoded values); extended `backend/src/config/env.ts` with the `db.*` env block; `backend/src/models/User.ts` (Sequelize `User` model: `id`, `email` (unique, validated), `passwordHash` — deliberately no plaintext password field, so the schema itself forecloses storing plaintext); `backend/src/models/User.test.ts` (happy path: create a user succeeds; failure path: creating a second user with the same email is rejected — covers this story's "signs up with an existing email" failure case at the model/constraint level). Tests run against an in-memory SQLite Sequelize instance rather than a live Postgres server (none available in this sandbox), per CLAUDE.md's unit-testing rule to mock external I/O; production config still targets Postgres.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 3/3 (1 existing `/health` test + 2 new `User` model tests); `npm audit` reported 0 vulnerabilities.
  - Notes: `npm install` initially surfaced 9 vulnerabilities (1 critical) via `sqlite3@5.1.7`'s transitive `node-gyp`/`tar` install chain — resolved by bumping `sqlite3` to `6.0.1`, which drops that vulnerable toolchain (native binary still builds and tests pass). A second, moderate vulnerability remained from `sequelize` 6.x's own pinned `uuid@^8.3.2` — no non-breaking `sequelize` upgrade fixes this upstream, so added a `package.json` `"overrides"` pin forcing `uuid` to `^11.1.1`; verified safe by confirming `sequelize` only calls `uuid`'s `v4()` with no arguments (the flagged advisory concerns a `buf` argument sequelize never passes). `npm audit` now clean. This is a data-contract-only step — no signup/login routes, no password hashing service yet (hashing deferred to the signup service, kept out of the model per one-responsibility-per-module). STORY-001 still in progress.

- [ ] STORY-001: User Registration and Login (Step 3 — password hashing service)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: Added `backend/src/services/passwordService.ts` — `hashPassword(plain)` (bcrypt, 12 salt rounds) and `verifyPassword(plain, hash)`, isolated from the `User` model per single-responsibility so the signup/login routes will call this service rather than hashing inline. `backend/src/services/passwordService.test.ts` covers the happy path (hash then verify the correct password succeeds, and the stored hash is never equal to the plaintext) and the failure path (verifying the wrong password against a valid hash returns false).
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 5/5 (3 prior tests + 2 new `passwordService` tests); `npm audit` reported 0 vulnerabilities.
  - Notes: First installed `bcrypt` (native), which pulled a critical `tar` CVE via its own pinned `@mapbox/node-pre-gyp` install chain with no non-breaking fix available (`npm audit fix` did not resolve it). Rather than pin another override, switched the dependency itself to `bcryptjs` — a pure-JS implementation with no native build step, so this class of install-time-toolchain CVE cannot recur for this package. Same public API (`hash`/`compare`), so `passwordService.ts` only needed its import changed. `npm audit` now clean. Still no signup/login HTTP routes — this is the hashing primitive only. STORY-001 still in progress.

- [ ] STORY-001: User Registration and Login (Step 4 — POST /auth/signup)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: Added `zod` for route-boundary validation. `backend/src/services/authService.ts` — `signup(userModel, email, password)`: checks for an existing user by email (throws `EmailAlreadyExistsError` if found), hashes the password via `passwordService`, creates the `User`. `backend/src/routes/authRoutes.ts` — `POST /auth/signup`: validates `{email, password}` with a Zod schema (email format, password min length 8) and returns 400 on failure before reaching business logic; calls `authService.signup`; returns 201 with `{id, email}` on success, 409 on `EmailAlreadyExistsError`, and forwards any other error to Express's error handler rather than swallowing it. `backend/src/app.ts` now takes a `userModel` parameter, mounts the auth router at `/auth`, and adds a generic error-handling middleware that logs a structured `{level, event, error_class}` line (per the Observability Framework's error-classification rule) and returns a bare `500 {error: "InternalServerError"}` without leaking internals to the client. `backend/src/server.ts` updated to build the real Postgres-backed `User` model and pass it into `createApp`. `backend/src/app.test.ts` updated to construct an in-memory-SQLite-backed model for the existing `/health` test (its signature changed). New `backend/src/routes/authRoutes.test.ts`: happy path (valid signup returns 201, the stored `passwordHash` is never the plaintext), failure path 1 (invalid email format returns 400, no row created), failure path 2 (duplicate email returns 409, still only one row for that email) — these are two of this story's three named failure paths.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 8/8 (4 test suites: `User`, `passwordService`, `app` (`/health`), `authRoutes` (`/auth/signup`)); `npm audit` reported 0 vulnerabilities.
  - Notes: Async Express 4 route handlers don't auto-forward thrown/rejected errors to error middleware, so unexpected errors are explicitly passed via `next(err)` rather than left to become an unhandled rejection — flagging this since it's a common correctness gap in Express 4 code, not obvious from reading the happy path alone. Login and password-reset (this story's third failure path — expired reset link) are not built yet; STORY-001 still in progress.

- [ ] STORY-001: User Registration and Login (Step 5 — POST /auth/login)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: Added `jsonwebtoken`. `backend/src/services/tokenService.ts` — `createSessionToken(userId, secret)` / `verifySessionToken(token, secret)`, 1h expiry. `backend/src/services/authService.ts` — added `login(userModel, email, password)`: looks up the user by email and verifies the password via `passwordService`; throws the same `InvalidCredentialsError` whether the email doesn't exist or the password is wrong, so a client can't learn which one failed. `backend/src/routes/authRoutes.ts` — added `POST /auth/login`: validates `{email, password}` with Zod, returns 200 `{token, user: {id, email}}` on success, 401 `{error: "InvalidCredentials"}` on either failure mode. `createAuthRouter`/`createApp` now take a `jwtSecret` parameter (threaded through from `env.JWT_SECRET`); `backend/src/server.ts` fails fast at startup with a clear error if `JWT_SECRET` is unset, rather than falling back to a hardcoded default secret. Updated `app.test.ts` and `authRoutes.test.ts` to pass a `'test-secret'` string (no real secret involved). New `authRoutes.test.ts` `POST /auth/login` suite covers the acceptance criterion directly: sign up then log in successfully (200, token returned), wrong password (401, no token), and an email that never signed up (401) — same status code for both auth failures, confirmed by asserting both return 401 rather than distinguishable codes.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 13/13 (5 test suites: `User`, `passwordService`, `tokenService`, `app`, `authRoutes`); `npm audit` reported 0 vulnerabilities.
  - Notes: This satisfies STORY-001's first acceptance criterion end-to-end ("sign up, then log in successfully") and covers the "existing email" and now "wrong credentials" failure paths. Still outstanding: password-reset flow (the "reset link expires" failure path and the second acceptance criterion), and the "log out" half of REQ-009 — a stateless JWT has no server-side logout by itself, so that needs a deliberate decision (short expiry alone vs. a token-blocklist) before it's built, not silently assumed. STORY-001 still in progress; Google/Apple OAuth remains deferred per the Step 1 scope note.

- [ ] STORY-001: User Registration and Login (Step 6 — password reset flow)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: `backend/src/models/PasswordResetToken.ts` (`userId`, `tokenHash` (unique), `expiresAt`, `used`). `backend/src/services/resetTokenService.ts` — `generateResetToken()` returns a random 32-byte hex token plus its SHA-256 hash; only the hash is ever persisted, mirroring the password-hashing rationale (a DB read can't hand out a usable token). `backend/src/services/passwordResetService.ts` (new file, kept separate from `authService.ts` per single-responsibility) — `requestPasswordReset(userModel, resetTokenModel, email)`: creates a 1-hour token record if the email matches a user, silently no-ops otherwise (the route always responds identically either way, so this can't be used to enumerate registered emails); `resetPassword(userModel, resetTokenModel, token, newPassword)`: validates the token is unused and unexpired (`InvalidOrExpiredResetTokenError` otherwise), updates the user's password hash, marks the token used. `backend/src/routes/authRoutes.ts` — added `POST /auth/password-reset/request` (always 200, generic message) and `POST /auth/password-reset/confirm` (200 on success, 400 on invalid/expired token). `createAuthRouter`/`createApp` now also take `resetTokenModel`; `server.ts` initializes it. Extracted `backend/src/testUtils/createTestApp.ts` (shared in-memory-SQLite app+model setup) since this was the third describe block needing the same boilerplate (Modular Composition Rule's 3-occurrence extraction threshold) — `app.test.ts` and `authRoutes.test.ts` now use it instead of duplicating setup. New `authRoutes.test.ts` password-reset suite: same generic response for a registered vs. unregistered email; full happy path (request → confirm → login with new password succeeds → login with old password now fails, proving the update took effect); expired token → 400 (this story's third named failure path); unknown token → 400.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 17/17 (5 suites); `npm audit` reported 0 vulnerabilities.
  - Notes: No email-delivery mechanism exists in this repo — the reset token is deliberately never returned in the API response (that would hand it to anyone who asked), so there is currently no way for a real user to receive it. Tests obtain the token by calling `requestPasswordReset` directly, standing in for the not-yet-built delivery step; this is flagged as a real gap, not solved here, since it needs a Mandrill-backed email service that doesn't exist yet in this codebase. With this step, STORY-001's second acceptance criterion (forgot-password → reset) and third failure path (expired reset link) are both covered by tests. Remaining before STORY-001 can be marked done: wire up actual reset-email delivery (or explicitly descope it with the user's sign-off), decide and implement the logout mechanism for REQ-009, and confirm the Google/Apple OAuth deferral from Step 1 is still acceptable.

- [ ] STORY-001: User Registration and Login (Step 7 — logout, pluggable reset-email delivery, HTTPS enforcement)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: Closing the three gaps flagged at the end of Step 6, per explicit user instruction to make the story complete. (1) **Logout** (REQ-009's other half): `backend/src/models/TokenBlocklist.ts` (`jti`, `expiresAt`); `tokenService.ts` now embeds a random `jti` in every issued JWT and exposes `revokeToken`/`isTokenRevoked` (`revokeToken` uses `findOrCreate` on the unique `jti` column, so revoking the same token twice is a no-op, not a duplicate row — this repo's idempotency rule); `verifySessionToken`'s return type changed from a bare `userId` to `{userId, jti, expiresAt}` (updated its one caller/test). New `backend/src/middleware/requireAuth.ts` (first use of the `middleware/` folder) — verifies the bearer token, rejects if missing/invalid/revoked (401), attaches `userId`/`tokenJti`/`tokenExpiresAt` to the request. `authRoutes.ts` gained `POST /auth/logout` (behind `requireAuth`), which revokes the presented token's `jti`. (2) **Password-reset email delivery**: `backend/src/services/notificationService.ts` defines an `EmailSender` interface plus a `ConsoleEmailSender` — a placeholder that logs that a reset was requested (never the token itself) instead of delivering anything, clearly commented as needing a real Mandrill-backed implementation before this can reach a real user (no Mandrill credentials exist in this repo, per CLAUDE.md's Tooling Assumptions). `passwordResetService.requestPasswordReset` now takes an `EmailSender` and calls it after creating the token record, rather than returning the token directly. `backend/src/testUtils/capturingEmailSender.ts` is a test double that records what was "sent," letting tests exercise the full `POST /auth/password-reset/request` → `/confirm` flow over HTTP instead of calling the service function directly (an improvement over Step 6's test approach). (3) **HTTPS enforcement**: `backend/src/middleware/requireHttps.ts` rejects any request that isn't `req.secure` or forwarded as `x-forwarded-proto: https` with 403; `app.ts` applies it (and sets `trust proxy`) only when `nodeEnv === 'production'`, so local dev/tests over plain HTTP are unaffected. Because `createApp`/`createAuthRouter` were approaching 5 positional parameters, both were refactored to take a single dependencies object (`AppDependencies`/`AuthRouterDependencies`) instead — `server.ts` and `testUtils/createTestApp.ts` updated accordingly.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 31/31 (6 suites, up from 5); `npm audit` reported 0 vulnerabilities.
  - Notes: **Genuine residual gap, not closed here:** there is no Docker (or any other way to run a real Postgres instance) available in this sandbox, so nothing in this story has ever been verified against the actual Postgres dialect `createSequelize()` targets — every test still runs against in-memory SQLite. This needs a real run against Postgres (locally or in a dev/staging environment) before shipping; I'm flagging it rather than silently treating SQLite-passing as equivalent proof. The `ConsoleEmailSender` is a real, working, tested code path — but it does not send an actual email; swapping in a Mandrill-backed `EmailSender` is still required before password reset works for a real user, and that needs credentials this repo doesn't have. `TokenBlocklist` rows are never pruned (no cleanup job) — acceptable at this scale but worth a follow-up. Google/Apple OAuth (REQ-001) remains deferred per the Step 1 scope note, unchanged by this step.

- [ ] STORY-001: User Registration and Login (formal scope decision — Mandrill email delivery and Google/Apple OAuth descoped)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: No code changed. Recording a scope decision the user made explicitly when asked directly: real password-reset email delivery (a Mandrill-backed `EmailSender`) and Google/Apple OAuth signup (REQ-001) are formally descoped from STORY-001's Definition of Done. Both are blocked purely on credentials this repo does not have access to (no Mandrill API key, no Google/Apple OAuth client credentials — consistent with CLAUDE.md's Tooling Assumptions, which explicitly say not to assume production credentials exist locally), not on remaining engineering effort. This aligns with STORY-001's literal written acceptance criteria, which test email signup→login and forgot-password→reset via a token, not actual inbox delivery or third-party OAuth.
  - Verification: User confirmed via direct question ("Descope both from STORY-001, formally").
  - Notes: This is not a silent scope narrowing — it was raised explicitly and the user chose it over "provide credentials now" or "leave both open, unflagged." If Mandrill or OAuth credentials become available later, `ConsoleEmailSender` (in `backend/src/services/notificationService.ts`) and the Step 1 OAuth deferral are the two integration points to revisit; no other code changes would be needed to slot them in, since both were already built behind an interface/DI seam for exactly this reason.

- [x] STORY-001: User Registration and Login (real-Postgres verification — closes the last open gap)
  - Date: 2026-08-18
  - Session: CC-20260817-r8w5
  - What changed: No application code changed. Per explicit user request to get to full confidence, closed the one remaining gap that was pure verification, not missing credentials: this story's code had only ever been exercised against in-memory SQLite. No Docker was available in this sandbox (checked: `docker` not found) and this shell was not elevated (checked: `net session` → access denied), so `choco install postgresql` (a Windows-service install) wasn't viable. Instead: downloaded the official EDB PostgreSQL 16.4 Windows x64 **binaries** archive (no installer, no service) to the session scratchpad, ran `initdb` and `pg_ctl start` as a plain user process on port 5433 — no admin rights, no system-wide install. Built the backend (`npm run build`) and ran a throwaway Node script (`verify-postgres.js`, deleted after use, never committed) that pointed a real `Sequelize({dialect: 'postgres', ...})` connection at this instance, synced the real schema, and drove the compiled `createApp()` through supertest exactly like the unit tests do, but against genuine Postgres: signup (201) → duplicate signup (409) → login (200, token) → wrong password (401) → logout (200) → second logout with the now-revoked token (401) → password-reset request (200) → reset token captured → reset confirm (200) → login with the new password (200) → confirmed the stored `passwordHash` in the real `users` table is never the plaintext. Afterward, stopped the Postgres process cleanly (`pg_ctl stop`, confirmed shut down) and deleted `verify-postgres.js` and `dist/` — the machine is back to its prior state, nothing left running or committed.
  - Verification: All 12 checks in the real-Postgres run passed (see script output in session transcript: "ALL CHECKS PASSED against real Postgres"). This is in addition to, not instead of, the existing `npm test` (31/31, SQLite-backed) and `npm audit` (0 vulnerabilities) — both still pass unchanged, confirmed again after `git status --short` showed only `PROGRESS.md` and the already-known untracked `backend/` scaffold, i.e. the Postgres verification left no residue.
  - Notes: This required zero code fixes — the Postgres dialect path worked correctly on the first real run, which is the strongest evidence available that `createSequelize()`'s production configuration and every Sequelize model/query in this story are dialect-correct, not just SQLite-compatible. With this, and the formal Mandrill/OAuth descope entry above, every item raised in the Step 6/7 confidence discussion is now closed one way or another (built-and-verified, or explicitly descoped by the user). STORY-001 is marked `[x]` here as the capstone entry for this verification; see the individual `[ ]` step entries above for what actually shipped. One immediately-following `npm test` run flaked with 4/6 suites timing out on `sequelize.sync()` — almost certainly Windows Defender scanning the several hundred newly-extracted Postgres binary files, since CPU load was still elevated (`wmic cpu get loadpercentage` → 26%, `MsMpEng.exe` active) and no code had changed since the prior clean run. Re-ran twice more immediately after with 31/31 passing both times (25.2s and 26.4s, back to normal speed) — recording this rather than silently re-running until green, since a flaky-look failure right after installing new binaries is exactly the kind of thing that deserves a paper trail instead of being quietly discarded.

- [x] STORY-001: User Registration and Login (wired into the Keysy Command Center dashboard)
  - Date: 2026-08-19
  - Session: CC-20260817-r8w5
  - What changed: Per explicit user request ("I want everything to reflect as and when done on the dashboard"), made STORY-001 actually visible on the Command Center built in STORY-000 — committing to git wasn't enough, since the dashboard reads `.colaberry/plan.json` and `.colaberry/progress.json`, and `plan.json` had never existed in this repo (no portal sync has ever happened; confirmed across every prior session's notes). Created `.colaberry/plan.json` containing only facts genuinely given across this session's task briefs — no invented dates, no invented requirement IDs, no invented backlog beyond what was named: `project` (name/descriptor as given), `stories` (STORY-001 with its real title/narrative/acceptance criteria; STORY-002 "Property Discovery Feed" and STORY-003 "Basic Property Search" as title-only stubs, release `r0` inferred from the release's own stated landing condition, which literally names their functionality), `requirements` (REQ-001 and REQ-009 exactly as stated, `kind`/`priority` as given), and `derived.guardrails` (the two guardrail statements from CLAUDE.md, given placeholder IDs `GUARDRAIL-TRANSIT-PASSWORDS`/`GUARDRAIL-AFFORDABILITY-DISCLAIMER` since no real REQ-numbers for them were ever provided to this session — not assumed from the dashboard's own SAMPLE data, which uses illustrative IDs by design). Deliberately omitted `schedule` and `releases` (no real calendar dates were ever given — inventing them would have broken the Project Management Gantt chart, which does no null-checking on release dates, and would have shown false precision). Also fixed `.colaberry/progress.json`: added the `verification: {state, commit, points}` object to both the existing STORY-000 entry (which had none — a gap from the STORY-000 build session, not something this session introduced) and the new STORY-001 entry, since that field is what the dashboard's Guardrails and Project Management tabs actually read (the existing `criteria: [{text, passed}]` shape is never read by any tab code — confirmed by grep). Real commit hashes used (`b23a901` for STORY-000, `6cb7802` for STORY-001), not placeholders. Deliberately did NOT add a `progress.totals` block (would require an honest total story/criteria count across the whole backlog, which isn't known) and did NOT create `.colaberry/manifest.json` (would silence the "no data has ever been synced" banner, which is still accurate — this data was hand-authored from session briefs, not synced from an actual portal).
  - Verification: Ran the real Command Center against these files with Playwright (`npx playwright`, installed fresh in the session scratchpad; browser: Chromium) via a local static server (`npx serve`). Project Management tab: STORY-001 row shows status "verified"; its detail page shows the real narrative and all 3 real acceptance criteria. Guardrails tab: both guardrails correctly show "not yet enforced" (intentional — `fulfilled_by` was left empty for both, since REQ-001's OAuth portion is genuinely unfulfilled and the transit-encryption guardrail's real-world truth depends on infrastructure, e.g. TLS termination, that doesn't exist yet; showing green here would have been a false claim about a security guardrail). Overview tab: headline counts honestly show "no data yet" (no fabricated totals). Data-freshness banner still reads "No data has ever been synced from the portal yet" (accurate, per the `manifest.json` decision above). Zero unexpected console/page errors — only the two expected 404s (`.colaberry/manifest.json`, still absent by design; browser's automatic favicon request). Screenshots saved to the session scratchpad (`pm.png`, `story001.png`, `guardrails.png`, `overview.png`). Server stopped cleanly afterward (`TaskStop`); nothing left running.
  - Notes: This is authored data, not portal-synced data — flagged in-session to the user, not silently presented as if a real sync occurred. `plan.json`/`progress.json` are not part of `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives`, so this entry is additional traceability, not a hard-gate requirement. STORY-002/STORY-003 appear on the dashboard as "no data yet" stubs, per the standing instruction not to build them yet — visible as placeholders, not fabricated progress. If real due dates, a full requirements list, or confirmed REQ-numbers for the two guardrails become available (e.g. once portal provisioning finishes, per this session's original "Read this first" note), `plan.json` should be regenerated from that source rather than hand-extended further.

- [x] Command Center dashboard — Overview/Project Management/Guardrails/Agents tabs updated for progress.json schema_version 2
  - Date: 2026-08-21
  - Session: CC-20260821-9fk3
  - What changed: A remote commit (`f32977a`, not from this session) replaced `.colaberry/progress.json` with a `schema_version: 2` shape — per-story `{criteria:[{text,passed}]}` instead of the old `{verification:{state,commit,points}}` object, and no `totals` block — which broke every tab that read `story.verification` directly. Added `CCData.deriveVerification(p)` in `assets/cc-data.js` to compute an equivalent `{state,commit,points}` from criteria pass/fail counts, falling back to the old `verification` object when present (keeps sample data and any future v1 data working). `joinStories()`, `assets/cc-tab-guardrails.js`, and `assets/cc-tab-agents.js` now call it instead of reading `.verification` off raw progress rows. `joinStories()` also now falls back to progress.json's `criteria[].text` as `acceptance_criteria` when `plan.json` doesn't have any yet (true today for STORY-002 and up, since `plan.json` was never updated past STORY-001). Added `CCData.computeTotals(plan, progress)` (prefers `progress.totals` when present, otherwise derives `stories_verified`/`stories_total`/`criteria_passed`/`criteria_total` from the joined stories); `assets/cc-tab-overview.js`'s Headline counts cards now use it, including the detail-view cards.
  - Verification: `node --check` passed on all four edited files. Wrote a throwaway jsdom script (not committed) that loads the real modules against the actual `.colaberry/plan.json` and `.colaberry/progress.json` and renders Overview, Project Management (list + STORY-002 detail), Guardrails, and Agents (list + detail): STORY-001 now shows "verified", STORY-002/STORY-003 show "not started" (derived, not hand-set), Overview headline counts show "1 of 3" stories verified and "3 of 9" criteria passed instead of "no data yet", and STORY-002's detail page shows its 3 acceptance criteria sourced from `progress.json` since `plan.json` has none for it.
  - Notes: Caught and fixed a bug in my own first pass — the Overview list view's "Points awarded" card briefly rendered the literal string `"null"` (since derived totals are always a truthy object even when `points_awarded` isn't derivable under schema v2) instead of "no data yet"; fixed before commit, confirmed by re-running the jsdom check. `plan.json` itself was not touched — it still only carries STORY-001 through STORY-003, so STORY-004 onward exist in `progress.json` but won't appear on the dashboard at all until `plan.json` is extended to declare them (`joinStories()` iterates `plan.stories`, not `progress.stories`); flagging this as the next gap rather than silently working around it.

- [x] .colaberry/plan.json extended with STORY-004 through STORY-015 so the Command Center actually shows them
  - Date: 2026-08-21
  - Session: CC-20260821-9fk3
  - What changed: Per user request to close the gap flagged above, added STORY-004 through STORY-012 and STORY-014/STORY-015 to `plan.json`'s `stories` array (13 new entries; STORY-013 has no data anywhere — `progress.json` itself skips from STORY-012 straight to STORY-014 — so it was left out rather than invented). Each new entry has only `id` and `title`; `release`, `owner`, `narrative`, and `acceptance_criteria` were deliberately left unset since no real value for any of them was ever given for these stories (unlike STORY-001–003, which came with real release/owner data). Titles were not invented from nothing: STORY-004's title ("Property Details and Match Score") is a direct quote from the existing code comment in `backend/src/types/property.ts`; the rest are short labels summarizing each story's own already-committed `progress.json` criteria text (e.g. STORY-010's criteria describe "affordability calculated... cost components... disclaimers visible" → "Affordability Calculator"). `acceptance_criteria` was deliberately left out of `plan.json` for all of them rather than copied over from `progress.json`, so `joinStories()`'s existing fallback (added in the prior entry) remains the single source of truth for that text instead of two files drifting apart.
  - Verification: `node -e "JSON.parse(...)"` confirmed `plan.json` is still valid JSON. Re-ran the same jsdom script used in the prior entry against the updated file: `joinStories()` now returns all 14 stories (STORY-001 through STORY-015 minus STORY-013), each with a derived verification state and 3 criteria pulled from `progress.json`; the Project Management list view now renders all 14 rows with real titles and "unassigned" (`—`) shown honestly for the missing release field; `computeTotals()` now reports "1 of 14" stories verified and "3 of 42" criteria passed (up from "1 of 3" / "3 of 9" before this change).
  - Notes: `release`/`owner` being unset means these 14 stories won't appear on the Project Management tab's release Gantt chart (which only reads `plan.releases`, still empty) — that's accurate, since no real release schedule exists for them yet, not a rendering bug. If real due dates, releases, or owners become available, this is the file to extend further rather than inferring them from story order.

- [ ] STORY-002: Property Discovery Feed (wired up and verified — real MLS integration still outstanding)
  - Date: 2026-08-21
  - Session: CC-20260821-9fk3
  - What changed: A prior, unlogged session had already built substantial STORY-002 code — `backend/src/routes/propertyRoutes.ts` (`GET /properties`, 503 on MLS failure), `backend/src/services/mlsClient.ts` (`StubMlsClient` — hardcoded sample properties, no real MLS API configured), `backend/src/models/Favorite.ts`, `backend/src/services/favoritesService.ts`, `backend/src/routes/favoritesRoutes.ts` (`POST/GET /favorites`, idempotent save via `findOrCreate` on the unique `(userId, propertyId)` index), `backend/src/types/property.ts`, matching test files, and a full frontend scaffold (`PropertyFeedPage`, `PropertyCard`, `LoginPage`, `propertyService`/`favoritesService`/`authService`, tests) — but none of it had ever been committed, and it wasn't wired into the running app: `app.ts` only ever mounted `/auth`, and `testUtils/createTestApp.ts` didn't know about the new `mlsClient` param or the `Favorite` model, so `propertyRoutes.test.ts`/`favoritesRoutes.test.ts` didn't even compile. This session finished the wiring rather than rebuilding: added `favoriteModel`/`mlsClient` to `AppDependencies` in `app.ts` and mounted `createPropertyRouter`/`createFavoritesRouter`; updated `createTestApp()` to accept an optional `mlsClient` (defaulting to `StubMlsClient`) and init/return `FavoriteModel`; updated `server.ts` to init the `Favorite` model and construct a `StubMlsClient` for the real app, with the same "placeholder, not real MLS" comment pattern already used for `ConsoleEmailSender`. Also updated `.colaberry/progress.json`'s STORY-002 entry to reflect reality: its first two criteria ("see a feed of properties," "saved property appears in favorites") are now genuinely true and marked `passed: true`; the third ("Trust: Property data is fetched from the MLS and displayed accurately") is left `passed: false` since `StubMlsClient` returns hardcoded fake data, not real MLS data — no MLS provider credentials exist in this repo (per CLAUDE.md's Tooling Assumptions), same category of gap as STORY-001's descoped Mandrill/OAuth integrations.
  - Verification: Backend — `npm run typecheck` (`tsc --noEmit`) passed clean; `npm test` passed 36/36 (8 suites, up from 31/31 across 6 — the 5 new tests are `propertyRoutes.test.ts`'s 2 and `favoritesRoutes.test.ts`'s 3, both of which now actually run instead of failing to compile); `npm audit` reported 0 vulnerabilities. Frontend — `npm run typecheck` passed clean; `CI=true npm test` passed 10/10 (4 suites: `App`, `LoginPage`, `PropertyFeedPage`, `PropertyCard`). Confirmed `frontend/src/App.tsx` already wires `LoginPage` → `PropertyFeedPage` on successful login (pre-existing from the unlogged session, not added here).
  - Notes: STORY-002 is left `[ ]` (not `[x]`) — its third acceptance criterion is genuinely unmet, not just unverified, and won't be until a real MLS integration replaces `StubMlsClient`, which needs credentials this repo doesn't have. This is a real gap being flagged, not a false completion claim. The Command Center dashboard will now show STORY-002 as "in progress" (2 of 3 criteria passed) rather than "not started," which is accurate. Frontend `node_modules` were already present in the working tree (not installed by this session) — `frontend/package.json`/`package-lock.json`/`tsconfig.json`/`public/`/`src/` are being committed for the first time here since they were untracked.

- [x] STORY-002: real end-to-end browser verification, found and fixed a real CORS bug
  - Date: 2026-08-21
  - Session: CC-20260821-9fk3
  - What changed: Per explicit user request to "fix everything" (narrowed via a clarifying question to: run the app end-to-end in a browser and fix real bugs found — not repo cleanup, not building further stories), actually ran the full stack instead of only trusting `npm test`. No Docker/`pg_ctl` available in this sandbox (same as the STORY-001 Postgres verification), so downloaded the same portable EDB PostgreSQL 16.4 Windows binaries to the session scratchpad, ran `initdb`/`pg_ctl start` as a plain user process on port 5433 (no admin, no service), created a `keysy` database, and synced the real schema (`User`, `PasswordResetToken`, `TokenBlocklist`, `Favorite`) via a throwaway script (deleted after use, never committed). Started the real backend (`npm run dev`) against that Postgres instance and the real frontend (`npm start`, CRA dev server) and drove them with Playwright (Chromium, installed fresh in the scratchpad): signup via direct API call (no signup UI exists yet — noted below, not built), then login through the real `LoginPage` form. **Found a real bug on the first pass**: every frontend→backend request failed with a CORS error (`No 'Access-Control-Allow-Origin' header`) — `app.ts` never set CORS headers, so the frontend on port 3000 could never call the backend on port 4000 in any deployment where they aren't same-origin. Fixed it: new `backend/src/middleware/cors.ts` (`createCorsMiddleware`) is a no-op unless a `CORS_ORIGIN` env var is set (config-from-env, not a hardcoded origin, per CLAUDE.md's 12-factor section), and short-circuits `OPTIONS` preflights with 204; wired into `app.ts` (new optional `corsOrigin` field on `AppDependencies`, applied before all routes) and `server.ts` (`env.corsOrigin` from `config/env.ts`'s new `CORS_ORIGIN` read). Added `backend/src/middleware/cors.test.ts` (3 cases: no-op with no origin configured, headers set + `next()` called for a normal request, `OPTIONS` preflight short-circuited with 204) since this is new production logic, matching the existing `requireHttps.test.ts` pattern. After the fix, re-ran the full flow: login succeeded, the property feed rendered all 3 stub properties with the affordability disclaimer, clicking "Save to favorites" turned the button into "Saved to favorites," and a fresh independent `GET /favorites` API call (new login, no browser state) confirmed `stub-1` was genuinely persisted server-side. One page-refresh check initially looked like a bug (favorited state disappears on reload) but turned out to be `authTokenStore.ts`'s existing, deliberately-commented design — the session token is kept in-memory only and cleared on refresh specifically to avoid a bearer token sitting in localStorage where an injected script could read it; correctly left alone, not "fixed."
  - Verification: Backend — `tsc --noEmit` clean; `npm test` 39/39 (9 suites, up from 36/36 — `cors.test.ts` is the 3 new tests); `npm audit` 0 vulnerabilities. Frontend — unchanged, not touched by this fix (the bug was backend-only). End-to-end — real Postgres + real Express + real CRA dev server + real Chromium via Playwright: signup (201) → login (200, token) → property feed render (3 properties, correct fields, disclaimer visible) → save favorite (button state updates client-side) → independent `GET /favorites` confirms `{"propertyIds":["stub-1"]}` server-side. Zero console/page errors after the CORS fix (there were exactly the CORS errors described above before it). Screenshots sent to the user directly (`02-after-login.png`, `03-after-save.png`). All temporary infrastructure torn down afterward: both dev server processes killed (confirmed ports 3000/4000 free), Postgres stopped (`pg_ctl stop`, confirmed port 5433 free), the ~1GB of extracted Postgres binaries + data directory deleted, throwaway sync/click-through scripts removed from the repo (the Playwright driver script itself lives only in the session scratchpad, never in this repo). `git status` after cleanup showed only the intended `cors.ts`/`cors.test.ts`/`app.ts`/`config/env.ts`/`server.ts` changes — no residue.
  - Notes: Real gap surfaced, not fixed here (out of the scope the user confirmed): the frontend has no signup page, only `LoginPage` — a new user cannot actually create an account through the UI yet, only via a direct API call, even though STORY-001's acceptance criteria are about signup-then-login. Also visually confirmed the property feed currently has zero custom CSS (functional, unstyled, plain browser defaults) — not a bug, just unpolished; flagged rather than silently addressed since visual design wasn't part of what was approved for this pass.

- [x] Prompt library: 5 new prompts (ai-home-finder-filters, match-explanation, notification-router, favorite-category-suggester, tour-request-validator), plus scripts/score_prompt.py and scripts/check_library.py
  - Date: 2026-08-24
  - Session: CC-20260824-t3xk
  - What changed: Built out `prompts/` as a versioned, scored prompt library for the Keysy app. `prompts/ai-home-finder-filters/` (v1.0.0 + v1.1.0 — natural-language search to structured filters), `prompts/match-explanation/` (compares buyer criteria against a property, returns matched/unmatched criteria + overall fit), `prompts/notification-router/` (classifies a listing event into one of 7 notification types + send/don't-send), `prompts/favorite-category-suggester/` (sorts a buyer's saved-property note into Favorites/Maybe/Want-to-Tour/Offer-Candidates), `prompts/tour-request-validator/` (checks a tour request for missing fields, an implausible date, or a spammy message before auto-confirming) — each with its own `eval.jsonl` (5-7 hand-authored cases, expected answers confirmed by the user turn-by-turn before the prompt file was written, per explicit instruction). Also wrote `scripts/score_prompt.py` (fills a prompt's `{{placeholders}}` from eval cases, calls the real Anthropic API via the SDK with the key loaded from `.env`, compares only the fields named in each case's `expected` object — case-insensitive/whitespace-trimmed for text, small tolerance for numbers — and reports a 0.0-1.0 score plus per-field failure detail; this file already existed untracked in the working tree from earlier in this session, verified rather than rewritten) and `scripts/check_library.py` (walks every `prompts/` folder, finds each one's newest `vX.Y.Z.md`, and for any prompt whose header says `status: ready`, checks it against the 5 rules in `prompts/CONTRIBUTING.md` — versioned filename, complete header, ≥3 eval cases, `last_eval` score ≥0.85, a named model — printing a table and exiting non-zero if a `ready`-claiming prompt actually fails; drafts are listed but not graded). Also wrote `prompts/CONTRIBUTING.md` (the library-ready rules in plain English) and `library_report.md` (a non-technical summary of the whole library, also copied to the user's Downloads folder).
  - Verification: Every prompt was scored for real against the live Anthropic API via `score_prompt.py`: `ai-home-finder-filters` v1.0.0 scored 0.71 (5/7) — root-caused to the model reading generic "houses" as implying `propertyType: single-family`, which the eval didn't expect; v1.1.0 (one isolated rule added, v1.0.0 left untouched) scored 1.00 (7/7), confirming the fix and nothing else moved. `match-explanation`, `notification-router`, `favorite-category-suggester`, and `tour-request-validator` each scored 1.00 on first write (5/5 cases each, including one deliberately-awkward edge case per prompt: empty buyer criteria, an unclassifiable event, an overlapping-signal note, and a fixed-order tie-break among multiple missing fields). `check_library.py` was run against the real library (correctly lists all 6 prompts — including the pre-existing, not-yet-scored `flag-listing-quality` — as `draft`, 0 ready, exit code 0) and against a throwaway scratch library with one valid `status: ready` prompt and one deliberately broken one (blank `model` field): correctly printed `pass`/`fail`, named the exact broken field, and exited 1; scratch test directory removed after.
  - Notes: Session ID was minted mid-session (`CC-20260824-t3xk`) — this conversation began before the session-start protocol's ID-minting step was followed, so this is the first entry under this ID for otherwise-earlier work in the same session. One process correction worth recording: after scoring `ai-home-finder-filters` v1.0.0 at 0.71, the propertyType fix was initially (incorrectly) edited directly into `v1.0.0.md` rather than saved as `v1.1.0.md`, which briefly violated the "never overwrite a version that has a recorded score" rule this same library's `CONTRIBUTING.md` now states explicitly. Caught before commit: `v1.0.0.md` was reverted to the exact content that earned its 0.71 score, and the fix was redone properly as `v1.1.0.md` — both versions and both real scores now coexist in the folder. Also worth noting: `match-explanation`'s output was deliberately designed as structured matched/unmatched criteria lists rather than a free-text explanation sentence, and `notification-router`'s `clarificationNeeded`-equivalent fields were kept to closed enum vocabularies — both because `score_prompt.py` only does exact/case-insensitive/tolerance comparison, so open-ended generated prose can't be scored reliably by this tool. None of the 6 prompts have been promoted to `status: ready` — that is a deliberate pending decision, not an oversight; `library_report.md` recommends it as a next step for the 5 that already qualify.

- [x] Wired ai-home-finder-filters and match-explanation into the real Keysy app (POST /search, full stack)
  - Date: 2026-08-25
  - Session: CC-20260824-t3xk
  - What changed: Per explicit user request to wire the prompt library into the running app ("full stack" scope, chosen over a backend-only or local-demo-only option), added a real `POST /search` feature. Backend: added `@anthropic-ai/sdk` and `dotenv` as new backend dependencies (deliberate add, not drive-by); `backend/src/config/loadEnv.ts` loads the repo-root `.env` before `config/env.ts` reads `process.env` (imported first in `server.ts`); `env.ts` gained `anthropicApiKey`/`anthropicModel`. `backend/src/services/anthropicClient.ts` is the first outbound-HTTP-call code anywhere in this backend — explicit timeout (20s) and capped retries (2) on the SDK client, catches and re-throws a generic `AnthropicUpstreamError` so raw SDK error detail never reaches a response body, logs only `error_class` server-side, per CLAUDE.md's external-call rules (there was no prior pattern in this repo to copy — `openclawCircuitBreaker.ts`, which CLAUDE.md cites as "the canonical pattern," does not exist anywhere in the repo). `StubAiClient` (always throws `AnthropicNotConfiguredError`) is used when no key is configured, mirroring the existing `StubMlsClient`/`ConsoleEmailSender` graceful-degradation pattern — the rest of the app still boots and works with no key set. `backend/src/services/promptLoader.ts` loads a scored `prompts/<name>/vX.Y.Z.md` file at runtime, strips its frontmatter, and fills `{{placeholder}}` tokens with the exact same `JSON.stringify`-per-value substitution `scripts/score_prompt.py` uses — production runs the literal file that was scored (pinned to `ai-home-finder-filters/v1.1.0.md` and `match-explanation/v1.0.0.md`), not a hand-copied TypeScript re-transcription, so the two can't drift apart. `backend/src/services/aiSearchService.ts` orchestrates: extract filters → deterministic pre-filter against the property feed (case-insensitive substring city match against the free-text address, since the stub data has no separate city field; price/bedrooms/bathrooms/propertyType/features are exact/threshold checks) → match-explanation per surviving property (capped at 10) → short-circuits to zero AI calls when `clarificationNeeded` is set. New `POST /search` route (`searchRoutes.ts`) validates the body with Zod (matching `authRoutes.ts`'s `safeParse`/`error.flatten()` convention exactly) and maps `AnthropicNotConfiguredError`/`AnthropicUpstreamError`/`MlsUnavailableError`/`AiResponseParseError` to 503/503/503/502 respectively. `Property` type (both backend and frontend copies — this repo has no shared-types package, kept in sync by hand per the type file's own existing comment) gained an optional `features?: string[]`; `StubMlsClient`'s 3 sample properties were given sample feature data (pool/garage/no-hoa/etc.) so feature-based filtering has something real to match against. Frontend: `frontend/src/services/searchService.ts` (same `fetch`+`AbortController`+timeout convention as `propertyService.ts`, just a longer 20s timeout since this does two AI round-trips server-side), `frontend/src/components/AiSearchBox.tsx` (new — a search box + results grid with its own state machine matching `PropertyFeedPage`'s `FeedState` pattern), `PropertyCard.tsx` gained an optional `matchInfo` prop that renders an overall-fit badge plus matched/unmatched criteria lines, and `PropertyFeedPage.tsx` now renders `<AiSearchBox>` above the existing full feed grid.
  - Verification: Backend — `tsc --noEmit` clean; `npm test` 52/52 (11 suites, up from 39/39 — 13 new tests across `aiSearchService.test.ts` and `searchRoutes.test.ts`, using hand-rolled fake `AiClient` objects per the existing `MlsClient` fake-injection test convention, not a mocking library); `npm audit` 0 vulnerabilities. Frontend — `tsc --noEmit` clean; `CI=true npm test` 14/14 (5 suites, up from 10/10 — 4 new `AiSearchBox.test.tsx` tests, `jest.mock` on `searchService` matching the existing `PropertyFeedPage.test.tsx`/`PropertyCard.test.tsx` convention). Real end-to-end verification (no Docker/`pg_ctl` available in this sandbox, same as the two prior STORY-001/002 verifications): downloaded portable EDB PostgreSQL 16.4 Windows binaries to the session scratchpad, ran `initdb`/`pg_ctl start` as a plain user process on port 5433, started the real backend (`npm run dev`) and real frontend (`npm start`) against it, and drove them with Playwright (Chromium, fresh install in the scratchpad). **Found and fixed a real bug on the first pass**: `loadEnv.ts` resolved `../../.env` from `backend/src/config`, landing on the nonexistent `backend/.env` instead of the real repo-root `.env` — an off-by-one path error (needs three `../`, not two) — so `ANTHROPIC_API_KEY` never reached `process.env` and every real search returned `503 AiSearchNotConfigured`. Fixed the path; confirmed via the dev server's own startup log going from `injected env (0) from .env` to `injected env (4) from ..\.env`, then a direct `curl POST /search` against the live Anthropic API correctly returning real extracted filters (`city: "Springfield"`, `bedrooms: 3`, `features: ["pool"]`) and a real match explanation (`overallFit: "full-match"`, `matchedCriteria: ["city","bedrooms","pool"]`) for the one stub property that actually has a pool. Also verified directly: the clarification path (`userQuery` with no location → exact canned clarification string, zero properties returned, confirming the short-circuit skips the second AI call) and the Zod validation path (empty `userQuery` → `400 ValidationError`). Browser pass via Playwright: typed "3 bedroom homes in Springfield with a pool" into the real `AiSearchBox`, got a rendered result card with a "Full match" badge and "Matches: city, bedrooms, pool" — screenshotted. **Found a second, smaller issue from the screenshot, not from logs**: the AI search results rendered directly above the full unfiltered property feed with no visual separation, so the matched property appeared to duplicate itself with no explanation why. Fixed with two heading additions (`Search results` / `Browse all homes`) rather than hiding either grid; re-verified via a second screenshot and a second `tsc`+test pass (still clean/14-14) before moving on. All temporary infrastructure torn down afterward: both dev server processes killed by port (3000/4000 confirmed free), Postgres stopped cleanly (`pg_ctl stop`, confirmed port 5433 free), the ~1.5GB of extracted Postgres binaries, data directory, and the scratchpad's throwaway Playwright install/driver script/screenshots all deleted — none of this was ever committed.
  - Notes: `Property.address` being one free-text string (no structured city field) means city filtering is a best-effort substring match, not exact — flagged in the code comment as a known limitation of the stub dataset, not something to silently paper over. `explainMatch()` deliberately echoes `buyerFilters.city` back as the property's city (rather than independently re-parsing it from the address) specifically so a property that passed the deterministic substring pre-filter can never come back "unmatched" on city from match-explanation's own exact-match rule — an intentional consistency choice between the two steps, not an oversight. `zipCode` is always sent as `null` to match-explanation, since the stub `Property` type has no zip field at all — a buyer asking for a specific ZIP will honestly get that criterion marked unmatched rather than silently ignored, which is correct given the underlying data gap. This is the first backend dependency on `dotenv` and the first code in `backend/src` to read from `prompts/` at runtime — both are new architectural surface worth knowing about, not just incidental additions.

- [x] Built UXDesigner_FieldGuide.html — Week 5 deep-dive knowledge base for the AI Solution Architect curriculum
  - Date: 2026-08-27
  - Session: CC-20260827-qc4k
  - What changed: Built a single self-contained HTML file at the repo root (`UXDesigner_FieldGuide.html`) — not under `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives`, but logged here per CLAUDE.md's broader "in-repo docs" rule. It's a knowledge-base-style deep dive (left topic nav, live keyword search over the page's own text, and an offline "Ask the guide" assistant — ~30 hand-written Q&A entries matched by keyword overlap, zero external API calls) teaching the UX judgment an AI Solution Architect needs to direct/evaluate/approve AI-generated UX work, taught entirely against one invented but concrete example: "CareLink," a patient referral/intake portal for a fictional healthcare network (Meridian Health Network). Contains a discipline primer (Nielsen's 10 heuristics, WCAG 2.1 AA/POUR, UX KPIs, a "good vs bad design" comparison, an "Architect's Review Lens" checklist) plus 9 full deliverable documents — Personas & JTBD, User Journey Map (with an unhappy path), IA/Sitemap, Annotated Wireframes (empty/loading/error/success states for 2 screens), Heuristic Evaluation (16 findings), WCAG 2.1 AA Checklist (22 criteria with measured contrast values), Design Token Sheet, Usability Test Plan & Script (incl. a full SUS-10 questionnaire), and an Executive Summary. Every document has its own branded cover, document-control strip, and Prepared/Reviewed/Approved sign-off block, and is individually downloadable as standalone HTML (`downloadHTML()`, which reuses the page's own `<style>` block rather than duplicating CSS), printable to PDF (`printDoc()`, isolates one section via a `print-hide` class toggle and `window.print()`), and — for the three tabular documents — exportable to CSV (`exportHeuristicsCSV`/`exportWcagCSV`/`exportTokensCSV`). All charts and diagrams (journey emotion curve, sitemap tree, wireframe states, severity/pass-rate donuts, bar charts) are hand-built inline SVG — no chart library, no CDN. Used only the mandated exact hex palette (Cherry Red #FB2832, Berry Blue #367895, Leaf Green #5BA63C, Amber #E8920C, the specified neutrals, and the given dark-mode variants) via CSS custom properties with a `prefers-color-scheme` + manual `data-theme` toggle, and Roboto/Roboto Mono (Google Fonts link with system-ui/Arial fallback for offline use). No emoji anywhere. The Colaberry logo was fetched live from `https://enterprise.colaberry.ai/colaberry-logo-transparent.png` and embedded inline as a base64 data URI in 18 places (9 document covers + 9 document footers) plus the page header/footer — 20 embeds total.
  - Verification: Downloaded the logo and confirmed a valid PNG signature (291×82, RGBA) before use. A first attempt to hand-type the ~25KB base64 string directly into an Edit call was caught as unreliable and reverted — instead wrote a Node script that copies the exact base64 text from the downloaded file into every logo placeholder, then verified via a second Node script that all 20 embedded data-URI payloads are exactly 25,324 characters (byte-identical to the source), so no corrupted logo shipped. After building all sections, ran Node validation scripts against the finished file: HTML tag balance (17 `<section>`, 26 `<table>`, all matching opens/closes; 3 `<script>` tags balanced); both real `<script>` blocks extracted and parsed with `new Function()` with zero syntax errors; the `deepdive-metadata` JSON script tag parsed cleanly via `JSON.parse`; all 35 in-page `<a href="#...">` anchors cross-checked against the 17 actual section ids — zero broken links, zero orphaned sections. Opened the finished file in the default browser (twice — once after the initial build, once after a final placeholder-comment cleanup pass) for a visual/functional check.
  - Notes: No `tsc`/test-suite verification applies — this is a static, dependency-free HTML deliverable with no build step, not application code, so validation was done via ad hoc Node scripts (tag balance, JS/JSON syntax, link-integrity) rather than an existing test runner. No `/directives` were touched and no secrets were introduced. Session ID `CC-20260827-qc4k` was minted for this entry since no session ID had yet been used today; this is the only entry under it so far.

## Order Support Assistant (order-status-mcp)

- [x] Environment verified and dev toolchain installed for MCP server work (Python, Node, uv)
  - Date: 2026-08-27
  - Session: CC-20260827-v9pl
  - What changed: No product code changed. Verified Python 3.12.10 and Node v24.18.0 already present; installed `uv` 0.12.7 via the official Windows installer script (`irm https://astral.sh/uv/install.ps1 | iex`, user-approved) since it was missing. Confirmed the pre-existing `order-status-mcp/` project (a uv-managed Python project with `mcp[cli]>=2.1.0`) imports cleanly and its one existing tool (`search_orders`) works — so no scaffolding was needed.
  - Verification: `uv --version` (via direct binary path, since a new shell hadn't picked up the updated PATH yet) reported `0.12.7`; `uv run python -c "import server"` exited 0 before any of this session's edits.
  - Notes: `mcp` 2.1.0 (the real, official `modelcontextprotocol/python-sdk`, confirmed via its own dist-info metadata) renamed the class formerly known as `FastMCP` to `MCPServer` — flagged to the user as a naming difference, not a broken/wrong package. Session ID `CC-20260827-v9pl` was minted mid-session (this catch-up entry covers environment-check work done earlier in the session, before any file changed).

- [x] Added live-tracking resource, human-escalation tool, and delay-explanation prompt to order-status-mcp
  - Date: 2026-08-27
  - Session: CC-20260827-v9pl
  - What changed: Per a user-reviewed and confirmed MCP-surface table, extended `order-status-mcp/server.py` with the two remaining primitive types (it previously had only the `search_orders` tool): (1) resource `tracking://orders/{order_id}` (`application/json`) returning a live carrier-tracking snapshot (status, last scan location/time, exception flag, revised ETA); (2) tool `escalate_order_to_agent(order_id, reason, customer_summary)` — validated `order_id` pattern, `reason` restricted to a 5-value enum, `customer_summary` 1-500 chars — that opens a human hand-off, deduped by `(order_id, reason)` so re-escalating the same issue is a no-op, not a duplicate ticket, per this repo's idempotency rule; (3) prompt `explain-shipment-delay(order_id)`, a named workflow a support agent invokes to draft a policy-grounded delay explanation from live order + tracking data. Extracted all sample data and lookup/business logic (previously inline in `server.py`) into a new `store.py` (`ORDERS`, `TRACKING_SNAPSHOTS`, `ESCALATIONS`, `find_orders`/`get_order`/`get_tracking`/`create_escalation`, `OrderNotFoundError`) so `server.py` only holds MCP registrations, per the Modular Composition Rule. Added `pytest` as a dev dependency (`uv add --dev pytest`) and `test_store.py` (10 cases: search matching/limit/no-match, unknown-order errors for lookup/tracking/escalation, escalation happy path, and idempotency — same `(order_id, reason)` returns the same escalation without duplicating, a different reason opens a separate one).
  - Verification: `uv run pytest -q` — 10/10 passed. `uv run python -c "import server"` — imports cleanly. Manually drove all three new primitives in-process (`mcp.call_tool`, `mcp.read_resource`, `mcp.get_prompt`): tracking resource returns the expected JSON snapshot for `ORD-1005`; escalating `ORD-1005`/`delivery_exception` twice returns the same `escalation_id` with `already_escalated` false→true on the second call (no duplicate created); the prompt renders a grounded delay-explanation request; reading tracking for an unknown order (`ORD-9999`) raises cleanly instead of returning fabricated data.
  - Notes: Tracking and escalation data are simulated in-memory (`store.py`), standing in for a real carrier API (UPS/FedEx/USPS or an aggregator) and a real ticketing system (Zendesk/Freshdesk/etc.) — both integration targets were explicitly flagged as unconfirmed with the user during the MCP-surface review and are still open. `escalate_order_to_agent`'s escalation store is process-memory only (resets on restart) — acceptable for tonight's build, not yet a durable store; flagging as the next gap rather than silently treating in-memory state as production-ready. No `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched.

## Keysy — Property Tour MCP Server (backend/src/mcp)

- [x] Added tour-scheduling MCP server (resource + tool + prompt) into the real Keysy backend, reusing its actual services
  - Date: 2026-08-27
  - Session: CC-20260827-v9pl
  - What changed: User redirected the night's MCP-server work from the standalone `order-status-mcp` project to Keysy itself, then chose (via `AskUserQuestion`) a fresh, Keysy-grounded MCP surface over relocating the order-status one, and TypeScript-inside-`backend/` over a second Python project, specifically to reuse Keysy's real services rather than duplicate or indirectly call them. Grounded the gap analysis in actual code (an `Explore` subagent survey, not `plan.json`'s story titles): real backend today is auth + property feed (stub MLS) + AI search + favorites; STORY-004 through STORY-015, including tour scheduling (STORY-008), have zero backend code. Per a user-reviewed and confirmed table, built: (1) resource `keysy-property` (`keysy://properties/{property_id}`, `application/json`) — a single-property lookup that didn't exist before (`GET /properties` only ever returned the whole feed); (2) tool `schedule_property_tour(property_id, requested_datetime, buyer_email, notes?)` — Zod-validated (`requested_datetime` ISO 8601, `buyer_email` email format, `notes` ≤300 chars), rejects an unknown property or a past datetime, idempotent via a `findOrCreate` on a new unique `(propertyId, buyerEmail, requestedAt)` index so re-scheduling the same tour is a no-op; (3) prompt `confirm-property-tour(property_id, requested_datetime)` — drafts a buyer-facing tour confirmation grounded in real property data. New files: `backend/src/models/TourRequest.ts` (Sequelize model, mirrors `Favorite.ts`'s pattern), `backend/src/services/propertyLookupService.ts` (`getPropertyById`/`PropertyNotFoundError`, shared by the resource and the tool), `backend/src/services/tourService.ts` (`scheduleTour`/`InvalidTourDatetimeError`), `backend/src/mcp/keysyMcpServer.ts` (`createKeysyMcpServer(deps)` — registrations only, reuses the real `mlsClient`/`tourService`), `backend/src/mcp/runKeysyMcpServer.ts` (stdio entry point wiring the real Postgres `Sequelize` connection and `StubMlsClient`, same placeholder-flagging pattern as `server.ts`). Added `@modelcontextprotocol/sdk@^1.30.0` as a new backend dependency (deliberate add; 0 vulnerabilities per `npm install`).
  - Verification: `npm run typecheck` (`tsc --noEmit`) passes clean across the whole backend. `npx jest` — 64/64 passed (up from 59/59): 7 new tests in `propertyLookupService.test.ts`/`tourService.test.ts` (happy path, unknown-property and past-datetime failures, idempotent-replay, and a same-property-different-time case creating a separate row — all against an in-memory SQLite `TourRequest` model, matching this repo's existing model-test convention) plus 5 new tests in `keysyMcpServer.test.ts` that drive the real `McpServer` through a real `Client` over the SDK's `InMemoryTransport` (not a hand-rolled fake) — resource read returns the correct property JSON with `mimeType: application/json`; the tool creates a tour and returns `already_scheduled: false`; calling it again with identical arguments returns the same `tour_id` and `already_scheduled: true` with the row count still 1; an unknown property returns `isError: true` instead of throwing; the prompt's drafted message contains both the real property address and the requested datetime.
  - Notes: Hit and resolved two real TypeScript issues, both worth recording since they're easy to hit again in this SDK version: (1) `server.registerTool`'s zod-based `inputSchema` caused `TS2589 Type instantiation is excessively deep` — root cause was importing `z` from top-level `'zod'` (3.25.76's top-level export is v4-flavored) while the SDK's own `zod-compat.ts` types its `AnySchema` against `'zod/v3'` specifically; fixed by importing `{ z } from 'zod/v3'` in `keysyMcpServer.ts`, which structurally matches what the SDK expects. (2) The same error reappeared only inside `ts-jest` (not in a full-project `tsc --noEmit`) because ts-jest type-checks each test file's dependency graph in relative isolation; fixed by switching `jest.config.js`'s transform to `ts-jest` with `isolatedModules: true` (ts-jest skips its own type-check, relying on `npm run typecheck` as the authoritative gate, which still covers every file including tests since `tsconfig.json` has no test-file exclusion) — this throws a one-line deprecation warning (ts-jest wants `isolatedModules` set in `tsconfig.json` instead, removed in ts-jest v30) that was deliberately left as-is rather than bumping a global compiler option without separately re-verifying its effect on the rest of the codebase; low-priority cleanup, not a functional gap. Real gaps, not solved here: `runKeysyMcpServer.ts` has not been run end-to-end as an actual stdio child process against a real Postgres instance (no Postgres available in this sandbox) — verification is at the `InMemoryTransport`+SQLite level only, the same category of gap this repo already logs for STORY-001/002's Postgres dialect and is a known, explicit follow-up, not a silent gap. `StubMlsClient` still means tour scheduling is only ever validated against 3 fake properties, same pre-existing limitation as the rest of the app. No `/frontend`, `/scripts`, `/nginx`, or `/directives` paths were touched; `.colaberry/plan.json` was not updated to reflect STORY-008 progress (not asked for this pass).

- [x] Added `notifications/progress` support to `schedule_property_tour`
  - Date: 2026-08-31
  - Session: CC-20260831-7k2m
  - What changed: Surveyed both MCP servers in this repo (the standalone `mcp-server/` and this one) for a tool with a real loop to instrument — neither had one, so rather than fabricate busywork, asked the user and got direction to treat `scheduleTour`'s 3 existing sequential stages (property lookup, datetime validation, `findOrCreate`) as the reportable steps. `backend/src/services/tourService.ts`'s `scheduleTour()` now takes an optional `onProgress` callback (new `ScheduleTourProgress`/`OnScheduleTourProgress` types) fired at each of those 3 real stages with a running step count, a real total (3), and a human-readable message naming the stage. `backend/src/mcp/keysyMcpServer.ts`'s `schedule_property_tour` handler reads `extra._meta?.progressToken`; when absent, `onProgress` is never passed (literally `undefined`) so the tool behaves exactly as before with zero overhead — a one-line comment documents why. When present, it wires the callback to `extra.sendNotification({ method: 'notifications/progress', ... })`. The tool's return value is unchanged.
  - Verification: `npm run typecheck` (`tsc --noEmit`) passes clean. `npx jest tourService.test.ts keysyMcpServer.test.ts` — 12/12 passed, including 2 new tests: one asserting `scheduleTour`'s callback fires with the exact 3 step/total/message values, one driving the real `McpServer` through a real `Client` over `InMemoryTransport` with `client.callTool(..., { onprogress })` and asserting the client receives the same 3 `notifications/progress` messages. User reviewed the full diff and approved before it was applied.
  - Notes: This is in the Keysy backend MCP server (`backend/src/mcp/`), not the standalone `mcp-server/` currently connected to MCP Inspector from the prior turn — they're two separate servers in this repo. To see it live, Inspector needs to be pointed at `backend/src/mcp/runKeysyMcpServer.ts` instead, which requires a live Postgres connection (unlike the standalone one) — not yet done this session.

## Standalone MCP Server (mcp-server/)

- [x] Added roots-based filesystem access enforcement, gated behind a new `read_property_document` tool
  - Date: 2026-09-02
  - Session: CC-20260902-f4xk
  - What changed: User asked for real MCP "roots" enforcement on any filesystem-touching tool in this project. Surveyed both MCP servers in the repo first — neither `mcp-server/` nor `backend/src/mcp/keysyMcpServer.ts` had a tool that touched the filesystem at all, so there was nothing yet for a roots guard to protect. Per the user's own choice (via `AskUserQuestion`, limited to `mcp-server/`), added a new tool, `read_property_document` (`mcp-server/src/tools/readPropertyDocument.ts`), that reads a stub disclosure/inspection document from `mcp-server/property-documents/{property_id}/{document_name}` — the first and, as of this entry, only filesystem-touching tool in either server. Built the guard itself as a standalone, reusable module: `mcp-server/src/security/pathRootsGuard.ts` (`assertPathWithinDeclaredRoots`). It asks the connected client for its declared roots via the real MCP `roots/list` request (not a hardcoded directory list), resolves the candidate path with `fs.realpath` (collapses `../` segments and dereferences symlinks in one step) and does the same to every declared root (a root can itself be a symlink), then compares only the two resolved paths — fails closed (denies) if the client never declared the `roots` capability, `roots/list` failed, or it declared zero roots. A denial returns a normal `{content, isError: true}` tool result rather than throwing, and emits an MCP `warning`-level log notification with the stable event name `roots_access_denied`, the requested path, and the reason. Added `mcp-server/src/data/propertyDocuments.ts` (stub property→directory lookup) and two sample on-disk files (`property-documents/p-1/disclosure.txt`, `property-documents/p-2/inspection-report.txt`). Wired the tool into `mcp-server/src/server.ts`. Documented the mechanism and the exact manual attack checklist to run in the Inspector (in-root `..`, out-of-root `..` traversal, a symlink escape, and the no-declared-root fail-closed case) in a new "Security: filesystem roots enforcement" section of `mcp-server/README.md`.
  - Verification: `npx tsc --noEmit` passes clean in `mcp-server/`. `npm start` boots the server cleanly with the new tool registered (manual run confirmed the normal "running and waiting for a client to connect" line, no errors). No automated test runner exists in `mcp-server/` yet (no test framework in its `package.json`, no prior `*.test.ts` files there), so this ships without automated tests; the manual Inspector attack checklist in the README is the verification path, to be run by the user by hand.
  - Notes: `backend/src/mcp/keysyMcpServer.ts` was checked and confirmed to have no filesystem-touching tool either; left untouched, out of scope per the user's own scoping choice. Separately, and unrelated to this change: `Anthropic course.txt` in the repo root was found to contain what looks like a live plaintext Anthropic API key — flagged to the user directly (rotate + remove), not something this entry's code touches.

## Keysy Home Buying App

- [x] STORY-001: closed the frontend gap flagged in the 2026-08-21 audit — Signup, Forgot/Reset Password, and Logout UI
  - Date: 2026-09-02
  - Session: CC-20260902-j2qw
  - What changed: A read-only audit at the start of this session (git log, PROGRESS.md, `.colaberry/plan.json`/`progress.json`, and the actual source tree) found STORY-001's backend fully built and Postgres-verified, but the frontend only ever had `LoginPage` — no way for a real user to sign up, recover a forgotten password, or log out through the UI, even though the backend has had working `/auth/signup`, `/auth/password-reset/request`, `/auth/password-reset/confirm`, and `/auth/logout` routes since 2026-08-18. Closed that gap, backend untouched. New frontend files: `pages/SignupPage.tsx` (signup, then chains into `login()` so a successful signup lands the user straight in the app per the story's own acceptance criterion; if the follow-up login call fails after a successful signup, sends the user to the login form instead of leaving them stuck), `pages/ForgotPasswordPage.tsx` (always shows the same generic confirmation regardless of whether the email is registered, matching the backend's anti-enumeration design), `pages/ResetPasswordPage.tsx` (takes a `token` prop sourced from a `?token=` URL query param — the shape a real emailed reset link would use once Mandrill delivery is wired up later), `components/AppHeader.tsx` (adds the "Log out" affordance that didn't exist anywhere in the UI). `services/authService.ts` gained `signup`, `logout`, `requestPasswordReset`, `confirmPasswordReset`, and matching typed error classes (`EmailAlreadyExistsError`, `InvalidOrExpiredResetTokenError`), following the existing `login()` function's fetch+`AbortController`+timeout convention exactly. `App.tsx` now runs a small view-state machine (`login` / `signup` / `forgot-password`, plus a URL-token check that takes priority over everything else) instead of the previous hard-coded two-screen toggle, and renders `AppHeader` above `PropertyFeedPage` once logged in. `LoginPage.tsx` gained "Sign up" and "Forgot password?" links wired to that state machine.
  - Verification: `tsc --noEmit` clean (backend and frontend). Frontend `CI=true npx react-scripts test --watchAll=false` — 34/34 passed across 9 suites (up from 24/24 across 5 before this change): new `SignupPage.test.tsx` (3 cases — happy path incl. the login hand-off, duplicate-email failure, and the failed-follow-up-login fallback), `ForgotPasswordPage.test.tsx` (4 cases incl. the no-enumeration property), `ResetPasswordPage.test.tsx` (3 cases), `AppHeader.test.tsx` (1 case), plus `App.test.tsx` grew from 2 to 7 cases covering view navigation, logout, and a regression test (see below). Real end-to-end browser verification, since this is a UI change (per this repo's own rule and precedent from the 2026-08-21 STORY-002 CORS-bug session): backend has no code changes this session, so rather than re-download the portable Postgres binaries used for prior full-stack verifications, built a throwaway Express app (`backend/src/__verifyServer.ts`, deleted after use, never committed) using the exact same `createApp()` the real `server.ts` uses, backed by in-memory SQLite (the backend logic itself, unchanged, was already Postgres-verified in the 2026-08-18/08-21 sessions) — this is a narrower-scope substitute for the full Postgres pass, not a claim that the unchanged backend was re-verified against Postgres. Ran the real CRA dev server against it and drove both with Playwright (Chromium, installed fresh in the session scratchpad, never committed) through 20 assertions: signup → auto-login → feed; logout → back to login; re-login with the signed-up credentials; forgot-password for both a registered and a never-registered email (byte-identical generic response, confirming no enumeration); an invalid reset token rejected; a real reset token (captured from a throwaway debug endpoint standing in for the not-yet-built email delivery) succeeding, after which the old password fails and the new one works; re-signup with the same email correctly rejected with 409. Zero unexpected console/page errors. All temporary infrastructure torn down afterward: both dev processes killed, `backend/src/__verifyServer.ts` deleted (confirmed via `git status` — no residue), scratchpad Playwright install left in the session scratchpad only (never touched the repo).
  - Notes: **Found and fixed a real bug via the browser pass, not caught by any unit test**: `authView` (the login/signup/forgot-password view state) was never reset on logout, so a user who had visited "Sign up" and later logged out landed back on the Signup form instead of Login — confusing, and arguably a broken "log out" experience. Fixed in `App.tsx`'s `handleLogout` (now also calls `setAuthView('login')`) and added a regression test to `App.test.tsx` that would have caught it. Real, still-open gaps, not closed here: `ConsoleEmailSender` (unchanged, still a stub) means a real user still cannot receive a password-reset email — `ResetPasswordPage` is built and fully working against a real `?token=` link, but nothing delivers that link to a real inbox yet; this was already formally descoped from STORY-001 by explicit user decision on 2026-08-18, unchanged by this entry. Google/Apple OAuth remains descoped for the same reason. With this change, STORY-001's stated acceptance criteria ("sign up using email, then log in successfully" and "forget their password, then reset it") are now genuinely operable end-to-end through the UI, not just through direct API calls — closing the specific gap this session was scoped to.

- [x] STORY-001: real password-reset email delivery via Resend, closing the last open gap from the previous entry
  - Date: 2026-09-02
  - Session: CC-20260902-j2qw
  - What changed: The prior entry flagged that `ConsoleEmailSender` (log-only) meant no real user could actually receive a password-reset email. User chose Resend (free tier, no card required) over the other options presented (AWS SES, Postmark, SendGrid) and added `RESEND_API_KEY` to the repo-root `.env` (confirmed present via a boolean check only — its value was never printed, per explicit user instruction to never expose any part of an API key in output). Installed `resend` (`^6.25.0`) as a deliberate new backend dependency, 0 vulnerabilities. New `backend/src/services/resendEmailSender.ts` (`ResendEmailSender implements EmailSender`) — explicit 10s timeout (the SDK has no built-in timeout/AbortSignal support for `emails.send`, so this wraps the call in a manual `Promise.race`-style timeout), a stable `idempotencyKey` derived from a SHA-256 hash of the reset token itself (passed as Resend's own `Idempotency-Key` header field, so an accidental duplicate call with the same token can't double-send), and a typed `ResendUpstreamError` that never leaks the raw Resend error body — only `error.name`/`error_class` is logged server-side, matching `anthropicClient.ts`'s existing pattern exactly. `config/env.ts` gained `resendApiKey`, `resendFromEmail` (defaults to Resend's own no-verification-required `onboarding@resend.dev` sender), and `appBaseUrl` (defaults to `http://localhost:3000`, used to build the `?token=` reset link the email actually contains). `server.ts` now picks `ResendEmailSender` when `RESEND_API_KEY` is set and falls back to `ConsoleEmailSender` otherwise — same graceful-degradation pattern as `mlsClient`/`aiClient`, so the app still boots with no key configured. **Hardened `passwordResetService.ts` as part of this change** (a real gap this integration would have introduced if left alone): wrapped the `emailSender.sendPasswordResetEmail` call in `requestPasswordReset` with a try/catch that logs and swallows any failure instead of letting it propagate — without this, a registered email would 500 during a Resend outage while an unregistered email still 200s, which is exactly the kind of enumeration side-channel the route's "always identical response" design exists to prevent. The reset token row is still created either way, so a successful-but-then-retried send isn't lost.
  - Verification: `tsc --noEmit` clean. `npx jest` — 75/75 passed across 16 suites (up from 68/68 across 14): new `resendEmailSender.test.ts` (4 cases, `resend` module mocked — happy path incl. asserting the reset link and idempotency key are present, an API-level error response mapped to `ResendUpstreamError`, a network-level rejection mapped the same way, and same-token calls sharing one idempotency key) and new `passwordResetService.test.ts` (3 cases, real in-memory-SQLite `User`/`PasswordResetToken` models — a failing `EmailSender` does not make `requestPasswordReset` throw, the token row is still created despite the failure, and an unregistered email still short-circuits before ever calling the sender). **Real live-API verification, not just mocks**: per explicit user confirmation (via `AskUserQuestion`, since this fires a real external side effect), built a throwaway script (`backend/src/__verifyResend.ts`, deleted after use, never committed) that loads the real `.env` through the real `loadEnv.ts`/`config/env.ts` path and calls the real `ResendEmailSender` against the live Resend API, sending one actual password-reset email to `martomng2020@gmail.com` (the address the user specified for this test, not their own primary address). Output confirmed `injected env (9) from ..\.env` (proving the real `RESEND_API_KEY` was loaded, not a test double) followed by `SEND SUCCEEDED`. Script deleted immediately after; confirmed via `git status` that no residue remains.
  - Notes: `RESEND_FROM_EMAIL`/`resendFromEmail` currently defaults to Resend's own `onboarding@resend.dev` testing sender, which works with zero domain setup but is rate-limited and not appropriate for real production volume — verifying a real sending domain in the Resend dashboard and setting `RESEND_FROM_EMAIL` is a follow-up, not done here. Separately, while checking `.env` for the Resend key this session, found a stray unlabeled line in it (no `KEY=` prefix, ~109 characters) sitting right after the `RESEND_API_KEY` line — flagged to the user directly without printing its content; it doesn't break env parsing (dotenv skips unparseable lines) but shouldn't be sitting in a secrets file unexplained. Also unrelated to this entry's code: earlier in this session, confirmed (again without printing any part of either value) that `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` already exist in this same `.env` and are properly gitignored/never committed — meaning the Google half of STORY-001's OAuth descope could be un-descoped and built whenever the user wants; Apple OAuth remains blocked on credentials that don't exist yet. With this change, STORY-001 has no further known gaps against its own three written acceptance criteria — signup→login, forgot-password→reset (now including real delivery), and secure credential storage/transit — all working end-to-end through the real UI and a live third-party email provider.

- [x] STORY-002: real MLS integration via SimplyRETS, closing the last open gap (real-Postgres/real-MLS category)
  - Date: 2026-09-02
  - Session: CC-20260902-j2qw
  - What changed: `StubMlsClient` (3 hardcoded fake properties) was the last thing standing between STORY-002's third acceptance criterion ("Trust: Property data is fetched from the MLS and displayed accurately") and being genuinely true. Real production MLS access requires being a licensed broker with an MLS board membership (RESO Web API via Bridge Interactive/Trestle/etc.) — not obtainable here. Presented the user three honest options (that path if they had access, SimplyRETS's public developer demo API, or leaving it stubbed); user chose SimplyRETS. Verified the real API live before writing any code (`curl -u simplyrets:simplyrets https://api.simplyrets.com/properties`, publicly documented demo credentials, no signup needed) — pulled a full sample response and a 500-listing sample to learn the real field shapes, the real `property.subType` vocabulary (`SingleFamilyResidence`/`Condominium`/`Townhouse`/null — no multi-family subtype exists anywhere in the fixed ~65-listing demo dataset), and that `type: 'RNT'` marks rentals, which this buying app now filters out. New `backend/src/services/mortgageEstimate.ts` (`estimateMonthlyPayment` — simple 20%-down/6.5%/30yr P&I estimate, explicitly scoped as a placeholder for STORY-010's real calculator, not an attempt to duplicate it). `backend/src/services/mlsClient.ts` gained `SimplyRetsMlsClient` (alongside the existing `StubMlsClient`, same file-pairing convention as `anthropicClient.ts`'s real+stub pair) — HTTP Basic Auth, 10s timeout, one capped retry (2 attempts total) on any failure, a typed `SimplyRetsListing` interface covering only the fields actually read, and `mapListingToProperty()` which returns `null` (dropped from the feed, not thrown) for any listing missing a field our `Property` type requires — defensive handling since this is untrusted external data. `bathrooms` is computed as `bathsFull + bathsHalf * 0.5`; `features` is derived from `pool`/`parking.spaces`/`fireplaces`/`association.fee` since SimplyRETS has no single features array; `imageUrl` falls back to the existing `placehold.co` placeholder if a listing has zero photos (never observed in the live dataset, but not assumed permanent). `config/env.ts` gained `simplyRetsBaseUrl`/`simplyRetsUsername`/`simplyRetsPassword`, defaulting to the public demo values — not a hardcoded secret, since SimplyRETS itself publishes these for anyone to use, and documented as swappable via env vars for a real paid account later with no code change. `server.ts` now constructs `SimplyRetsMlsClient` unconditionally (no "unconfigured" fallback state makes sense here, unlike Resend/Anthropic, since the demo credentials always work out of the box) — `StubMlsClient` remains only as `testUtils/createTestApp.ts`'s default, keeping the existing test suite network-free and deterministic, per this repo's unit-testing rule.
  - Verification: `tsc --noEmit` clean. `npx jest` — 86/86 passed across 18 suites (up from 75/75 across 16): new `mortgageEstimate.test.ts` (3 cases) and `mlsClient.test.ts` (8 cases, `global.fetch` mocked with realistic SimplyRETS-shaped fixtures — correct field mapping, rentals excluded, a malformed listing dropped rather than crashing the whole feed, missing-photo fallback, real Basic Auth header construction, a non-ok response retried once then throwing `MlsUnavailableError`, a network rejection handled the same way, and a transient-failure-then-success case proving the retry actually recovers). **Real live-API verification, not mocks**: built a throwaway server (`backend/src/__verifyServer.ts`, deleted after use, never committed) wiring the real `SimplyRetsMlsClient` into the real `createApp()` (SQLite for auth/favorites, unrelated to this change), confirmed via direct `curl http://localhost:4000/properties` that it returns 43 real listings (65 total minus rentals) with real CloudFront photo URLs and correctly mapped fields. Then a full real-browser pass with Playwright (Chromium, scratchpad-only install): signed up, waited for actual property cards (not just the static page heading) to render, confirmed >3 properties (beating the old 3-property stub ceiling) and at least one image genuinely served from SimplyRETS's `cloudfront.net` CDN, saved a real MLS-backed property to favorites, logged out and back in with a fresh session, and confirmed the favorite persisted server-side against a real `mlsId`-derived property id (not `stub-1`). Zero unexpected console/page errors. All infrastructure torn down afterward — this time both dev processes needed a manual `taskkill //T` after `TaskStop` didn't actually kill the underlying `ts-node-dev`/CRA child processes (confirmed via `netstat` still showing ports 3000/4000 LISTENING after `TaskStop` reported success — noting this as a real gap in the stop mechanism used this session, not something to silently paper over), then confirmed both ports free and `backend/src/__verifyServer.ts` deleted with `git status` showing no residue. Updated `.colaberry/progress.json`'s STORY-002 third criterion from `false` to `true`, since it's now genuinely met.
  - Notes: **Honesty caveat, stated here and in code comments**: this is a real HTTP integration with real auth, real failure modes, and real (if demo) listing data — genuinely different from `StubMlsClient`'s hardcoded array — but the underlying ~65 listings are SimplyRETS's fixed public demo dataset, not live real-world MLS inventory. This is disclosed in `mlsClient.ts`'s class-level comment and should be disclosed to any real end user before this ships past a portfolio/demo context. Real, still-open gaps: no caching of the MLS response (refetched on every `GET /properties`) — fine at 65 listings from a free demo API, would need revisiting against a real paid provider with rate limits; the "multi-family" `PropertyType` value can never actually occur from this dataset (not a bug — SimplyRETS's demo set simply doesn't contain any). With this change, STORY-002 has no further known gaps against its three written acceptance criteria.

- [x] Built SoftwareEngineer_FieldGuide.html — Week 2 deep-dive knowledge base for the AI Solution Architect curriculum (catch-up entry)
  - Date: 2026-08-27
  - Session: CC-20260902-h6mz
  - What changed: No code changed this session. Filing the missing PROGRESS.md entry for a pre-existing, already-finished deliverable found sitting untracked in the working tree with no record anywhere in this file: `SoftwareEngineer_FieldGuide.html` (1969 lines), the engineering-judgment counterpart to the already-documented `UXDesigner_FieldGuide.html` (see the 2026-08-27 entry above) — same "AI Solution Architect" curriculum, same self-contained/dependency-free HTML pattern (left topic nav, offline keyword-search "Ask the guide" panel, per-document download/print, inline SVG charts, mandated color palette, no external chart libraries, no CDN). Dated 2026-08-27 to match its sibling guide, the only date evidence available. This entry exists solely to close the documentation gap before the file is committed, discovered while syncing a large backlog of previously-uncommitted-but-already-logged work onto the Keysy Command Center dashboard and into git.
  - Verification: File confirmed present and intact — `wc -l` reports 1969 lines; `<title>Software Engineer Field Guide — Colaberry Enterprise AI Leadership Accelerator</title>` confirms subject matter and program. No `tsc`/test-suite verification applies — static, dependency-free HTML with no build step, consistent with the UXDesigner sibling entry's own verification approach.
  - Notes: Whichever session actually built this file did not mint a Session ID or file a PROGRESS.md entry for it — a real process gap, not something this catch-up entry can retroactively fix beyond recording that the gap exists. No further content changes were made to the file itself.
